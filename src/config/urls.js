const mainUrl = 'https://jsonplaceholder.typicode.com';

export const urls = {
    users: `${mainUrl}/users`,
    posts: `${mainUrl}/posts`,
    comments: `${mainUrl}/comments`,
    albums: `${mainUrl}/albums`
}
  
